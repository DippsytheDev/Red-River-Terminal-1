// Frontend -> Backend configuration
// If your backend API is hosted on the same origin as this site, leave it as "".
// If it's hosted separately, set the full base URL, for example:
//   window.API_BASE_URL = "https://api.redriverterminal.com";
window.API_BASE_URL = "";

